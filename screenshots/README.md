# Public Screenshot Gallery

These screenshots are the public product gallery for the docs-only branch.
They must come from the actual modDJs Studio interface, not unrelated desktop captures.

Current files:

- `library-track-pool.png`
- `set-builder-workspace.png`
- `transition-editor-workspace.png`
- `export-studio-workspace.png`

Recommended screenshot order in `README.md`:

1. `library-track-pool.png`
2. `set-builder-workspace.png`
3. `transition-editor-workspace.png`
4. `export-studio-workspace.png`

Recommended caption direction:

- `library-track-pool.png`
  Show the real left-side track pool and search/filter surface from the app.
- `set-builder-workspace.png`
  Show target duration, preset selection, quick actions, and right-side inspector in one frame.
- `transition-editor-workspace.png`
  Shape overlap timing, inspect compatibility, and place recall slices.
- `export-studio-workspace.png`
  Follow the final print phase with mastering-aware export visibility.

Use production builds only.
Avoid debug states, diagnostics, development overlays, and missing-file warnings.
